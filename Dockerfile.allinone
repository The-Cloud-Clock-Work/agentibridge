# All-in-one AgentiBridge image: Python + embedded Redis + supervisord.
# Single container, single `docker run` command.
#
# Build:
#   docker build -f Dockerfile.allinone -t agentibridge:allinone .
#
# Run:
#   docker run -d -p 8100:8100 \
#     -v ~/.claude/projects:/home/appuser/.claude/projects:ro \
#     --name agentibridge \
#     agentibridge:allinone

FROM python:3.12-slim

# Runtime deps: Redis + supervisor + curl (healthcheck)
RUN apt-get update && apt-get install -y --no-install-recommends \
    redis-server \
    supervisor \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Non-root user
RUN useradd -m -u 1000 -s /bin/bash appuser

WORKDIR /app

# Python deps
RUN pip install --no-cache-dir \
    "fastmcp>=2.0" \
    "redis>=7.0" \
    "uvicorn[standard]>=0.30" \
    "httpx>=0.25" \
    "anthropic>=0.40"

# Copy application
COPY --chown=appuser:appuser agentibridge/ ./agentibridge/

# Copy supervisor config
COPY deploy/supervisord.conf /etc/supervisor/conf.d/agentibridge.conf

# Create directories
RUN mkdir -p /app/logs /data /home/appuser/.claude/projects \
    && chown -R appuser:appuser /app/logs /data /home/appuser/.claude

# Expose SSE port
EXPOSE 8100

# Environment defaults
ENV PYTHONPATH=/app \
    AGENTIBRIDGE_TRANSPORT=sse \
    AGENTIBRIDGE_HOST=0.0.0.0 \
    AGENTIBRIDGE_PORT=8100 \
    AGENTIBRIDGE_POLL_INTERVAL=60 \
    REDIS_URL=redis://127.0.0.1:6379/0

# Health check
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD curl -sf http://localhost:8100/health || exit 1

# supervisord manages both redis-server and agentibridge
CMD ["/usr/bin/supervisord", "-c", "/etc/supervisor/conf.d/agentibridge.conf"]
